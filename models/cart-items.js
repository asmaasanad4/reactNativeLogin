class CartItem {
  constructor(quantity, productPrice, productName, productImage, sum) {
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.productName = productName;
    this.productImage = productImage;
    this.sum = sum;
  }
}

export default CartItem;
